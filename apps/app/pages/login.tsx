import { ImgHeader, Layout, LoginForm } from '../components';

export const Login = () => {
  return (
    <Layout>
      <ImgHeader />
      <LoginForm />
    </Layout>
  );
};

export default Login;
